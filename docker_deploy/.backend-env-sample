POSTGRES_USER=aalto_user
POSTGRES_ADMINJS_USER=adminjs
# replace "RANDOM-PASSWORD" with a long random password
POSTGRES_PASSWORD=RANDOM-PASSWORD
# replace "RANDOM-PASSWORD" with a long random password and update the same password to ./db/init.sql
POSTGRES_ADMINJS_PASSWORD=RANDOM-PASSWORD
POSTGRES_DB=aalto_backend
POSTGRES_PORT=5432
# replace "RANDOM-SECRET" with a long random string
JWT_SECRET=RANDOM-SECRET
# replace "SECRET-KEY" with a key that will be required to create a new account
REGISTER_KEY=SECRET-KEY
# replace "RANDOM-SECRET" with a long random string
ADMINJS_SESSION_SECRET=RANDOM-SECRET
POSTGRES_HOST=db
ENVIRONMENT=openai
PROXYURL=http://proxy:8080
NODE_ENV=production