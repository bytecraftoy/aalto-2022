import { LoginForm } from '../../components/LoginForm';

export const Login = () => {
    return (
        <div className="flex-1 flex justify-center items-center">
            <LoginForm />
        </div>
    );
};
